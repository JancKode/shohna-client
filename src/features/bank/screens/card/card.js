import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Card = () => {
  return (
    <View style={styles.card}>
      <Text></Text>
    </View>
  );
};

export default Card;

const styles = StyleSheet.create({
  card: {
    width: 328,
    height: 210,
    backgroundColor: '#155587',
  },
});
